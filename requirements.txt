# ============================================================
# FlowGuard Backend — Runtime Dependencies (Render production)
# ============================================================
# Only packages required to serve the deployed FastAPI app.
# Dev/test/notebook tools live in requirements-dev.txt.

# --- ASGI server (required to run the app) ---
uvicorn[standard]>=0.29.0          # uvloop + httptools + websockets

# --- Web framework & validation ---
fastapi>=0.110.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.1

# --- Database & migrations ---
sqlalchemy>=2.0.0
alembic>=1.13.0
psycopg2-binary>=2.9.9

# --- Data processing (used by ML inference endpoints at runtime) ---
pandas>=2.0.0
numpy>=1.24.0

# --- Machine learning inference (loaded via joblib at request time) ---
scikit-learn>=1.3.0
xgboost>=2.0.0
joblib>=1.3.0

# --- Constraint-based optimization solver ---
ortools>=9.10.0
